({
    doInit: function(component, event, helper) {

        helper.getMDOVersion(component);
        helper.getOrgExpirationDate(component);
        helper.getApttusLicenseExpirationDate(component);


    }
})